import React from "react";
import { Typography } from "@mui/material";

const LiveQuizzes = () => {
  return (
    <div>
      <Typography variant="h4" gutterBottom>
        Live Quizzes
      </Typography>
      <Typography variant="body1">
        Here, students will see the live quizzes they can take.
      </Typography>
    </div>
  );
};

export default LiveQuizzes;
